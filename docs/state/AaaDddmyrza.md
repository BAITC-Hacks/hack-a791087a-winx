# @AaaDddmyrza — состояние

## Сейчас
База — `d454f61`; K0: безопасный live-launcher. 3D обязателен по новому требованию; интерфейс/engine остаются на P0.
Git email: adildaulet2005@gmail.com; зона: app/ui/AI/3D.

## Сделано
PLAN/IMPLEMENTATION_PLAN/IDEA/README согласованы: V1 (сцена) и V2 (A/B в сцене) обязательны без отсечения по сроку.
scripts/start_live.py запрашивает существующий ключ скрыто, без записи на диск; tests/test_live_launcher.py проверяет границы ввода.
docs/SECRETS.md описывает запуск, OneDrive, расходы и проверку live; AGENTS/HANDOFF/THIRD_PARTY обновлены.

## Не закончено
E1: validate_scenario/simulate — зона @alikhan.
U1/I1/A1, V1/V2, A2 — UI/AI/3D; реализация не выполнена.
C0/S1 — контракты поиска и детерминированный поиск не реализованы.

## Решения
TASK.md, DATASET.md и citysim/data.py остаются источниками правил и не меняются.
План: локальная сборка Three.js; Python передаёт готовые снимки сцены. JS ещё не реализован.
При сбое WebGL таблицы/fallback сохраняют доступ к сценарию, но не заменяют успешную 3D-приёмку на поддерживаемых браузерах.
Расширения полного поиска, свободного текста и экспорта необязательны.

## Грабли
Папка в OneDrive: .gitignore не мешает синхронизации .env; предпочтителен скрытый ввод без файла.
Тесты Python не подтверждают реальный рендеринг 3D; нужна браузерная проверка V1/V2.

## Проверка
`python run.py --check`: 15 тестов прошли; только фиктивный ключ, без платных API-запросов.
Скрытый ввод и HTTP200 проверены на фиктивном ключе; реальный ключ, модель, баланс и live-ответ не проверены.
Исправлена остановка launcher на Windows; unit-тесты прошли, повторная интерактивная проверка прервана пользователем.

## Следующий шаг
Начать U1 параллельно E1, затем пройти зависимости до проверенной V1/V2.
