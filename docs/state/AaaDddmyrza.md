# @AaaDddmyrza — начальная передача

## Сейчас
P0 передан; следующий этап U1. Git email: adildaulet2005@gmail.com.
Файл и начальный скелет созданы @alikhan, это не отчёт об авторской работе напарника.
Исходные документы: `93c2a24`; скелет: `git log -1 --format=%h -- docs/state/AaaDddmyrza.md`.

## Сделано
Готовы `app.py`, `citysim/ai.py`, общие типы `citysim/models.py` и `docs/API.md`.
Показаны baseline, районы, каталог; кнопка объяснения работает в demo.

## Не закончено
U1: выбор пяти мер, районы, бюджет, вывод ошибок; I1: подключение simulate.
A1: объяснение полного сценария; live API подключён, но не проверен реальным запросом.

## Решения
Своя зона: `app.py`, `citysim/ai.py`, тесты AI/UI и этот файл.
Engine и общие типы не менять; запросы писать @alikhan в `docs/HANDOFF.md`.
До E1 использовать локальные моки по API v1; явно маркировать их как моки.
OpenAI получает готовый SimulationResult, DEMO_MODE=1 исключает сеть.

## Грабли
validate_scenario/simulate пока NotImplementedError; baseline не является игровым решением.
Для городских мер передавать None, не строку «город».
Нельзя требовать по одной мере из всех пяти направлений: ограничения заданы в DATASET.md.

## Проверка
`python run.py --check` → 8 тестов OK в стартовом скелете; `python run.py` → UI.
Без .env и ключа: бюджет 100, baseline 52.5577, Ncrit=2, кнопка показывает demo.

## Следующий шаг
Реализовать U1: форму ровно пяти Decision с выбором района и отображением ValidationResult на моках API v1.
