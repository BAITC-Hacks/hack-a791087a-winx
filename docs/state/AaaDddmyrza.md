# @AaaDddmyrza — состояние

## Сейчас
V1-P: 3D-прототип на базе `cee214e`, ветка `codex/3d-prototype`.
Пользователь разрешил коммит/push; коммит пакета: `git log -1 --format=%h -- app.py`.
Git email: adildaulet2005@gmail.com; зона: app/ui/AI/3D.

## Сделано
Получены E1/E2/C0 напарника; полный набор с прототипом — 70 Python-тестов.
components/city3d: локальная Three.js-сборка, 5 районов, камера, выбор, карточка и fallback.
ui/scene.py передаёт готовые числа, проверяет селекторы и события; тесты в tests/test_scene_payload.py.
app.py показывает baseline в 3D и выбор 10 показателей; README/PLAN/HANDOFF/THIRD_PARTY обновлены.

## Не закончено
U1/I1/A1: форма пяти решений, снимки сценариев, паспорт результата и AI-анализ сценариев.
S1 у напарника; полная интеграция C0/V1/V2/A2 остаётся открыта. Прототип не закрывает весь V1.
Реальная потеря WebGL-контекста и физический pinch на телефоне требуют финальной браузерной проверки.

## Решения
TASK.md, DATASET.md, citysim/data.py, engine и API напарника не менялись.
Геометрия условная; столбцы показывают только готовые показатели 0–100, риска на 40.
Baseline использует поля C0; остальные состояния/события ещё подключаются. Node нужен только для сборки.
3D и демо работают без API-ключа; платных вызовов в проверках не было.

## Грабли
Модуль JS может выполняться заново при rerun: состояние камеры хранится на странице по стабильному view_key.
Multitouch/cancel/drag не должны вызывать выбор района; добавлены регрессионные JS-тесты.
После изменения frontend выполнить npm run build и перезагрузить приложение.

## Проверка
`python run.py --check`: 70 тестов; `npm test` в frontend: 4 теста; `npm run build` проходит.
WebGL, выбор JS→Python, слой S1/E1, desktop/390px просмотр проверены в in-app Chromium.
Локальный preview: http://127.0.0.1:8514, DEMO_MODE=1; обычный запуск: python run.py.

## Следующий шаг
U1: форма пяти решений с готовым движком E1, затем I1 и подключение результатов к сцене.
