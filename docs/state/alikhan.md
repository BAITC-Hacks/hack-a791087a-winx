# @alikhan — состояние

## Сейчас
S1 опубликован в main `f9e5797`; проверен вместе с PR #2 `017d6b7` (U1/I1/A1).
PR #2 ещё открыт: два конфликта документов; в main остаётся прежний UI baseline.
Коммит сводки: `git log -1 --format=%h -- docs/state/alikhan.md`.

## Сделано
Совместная копия кода f9e5797 + PR #2: 119 Python-тестов, 4 JS-теста OK.
AppTest: форма → A → S1 двумя раундами → паспорт/demo best; A сохраняется.
Chromium: пример 95/5/56.5431/0, Нура S1=48 и S2=43.75 в паспорте.
docs/NEXT_PARALLEL_STEPS.md: аудит, состояние веток, разделение следующих задач.

## Не закончено
Объединить PR #2: конфликты docs/PLAN.md и docs/HANDOFF.md, код без конфликтов.
V1 показывает только baseline; серверный A2, его UI и V2 ещё не реализованы.

## Решения
Предложено: @alikhan — citysim/reviewer.py/test_reviewer.py; напарник — UI/3D.
Официальная формула и engine сохранены; новые Score считает только simulate.
max_changes=0/1; locked — точные пары исходного A; оба раунда проверяются против A.
EPS=1e-9 относительно A; до 20 уникальных кандидатов, без обещания оптимума.
context_digest проверяет согласованность снимка/датасета; это не аутентификация.

## Грабли
previous брать неизменённым из S1; ручные экземпляры без digest отклоняются.
Более 20 unique (с previous) → ValueError до новых расчётов; A/повторы не считаются.
Не передавать simulate в текущий scene builder: он пометит сценарий как baseline.
В A2 best.score_delta — от baseline, не выигрыш B относительно A.

## Проверка
main: 100 тестов; совместная копия: unittest discover -s tests -v → 119 OK.
Эталон A: 95 / 56.54307; best из 20: 80 / 56.69056 / Ncrit=0 (M5 → M11/Нура).
Генерация + review20: медиана 17.53 мс, диапазон 16.90–18.38 мс, 10 прогонов.
Два раунда по 10 дают тот же best, что один прогон 20; исходный A сохраняется.
JS 4 OK; live AI здесь не вызывался. Новый чистый клон остаётся частью R.

## Следующий шаг
Объединить PR #2; затем A2 backend параллельно V1 напарника по NEXT_PARALLEL_STEPS.
