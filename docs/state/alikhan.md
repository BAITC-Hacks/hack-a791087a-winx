# @alikhan — состояние

## Сейчас
Финальная полировка и R: интерфейс/README готовы, завершается приёмка чистого клона.
База main: 7f8bfb7 (объединены A2 UI/V2).

## Сделано
Понятные подписи действий A/B, мер, показателей и ограничений; риски B видны сразу.
3D: имена показателей, читаемая панель и узкие экраны, локальный bundle пересобран.
DEMO_MODE=auto: ключ+модель включают OpenAI; без них локальный режим.
Live A1 получает исходные и итоговые показатели; формулы engine не изменялись.
README/DEMO: установка своего API token, OpenAI, fallback и точный offline-пример.

## Не закончено
Финальный чистый клон после публикации и запись результатов R в docs/ACCEPTANCE.md.

## Решения
Все числа считает engine по DATASET; API объясняет и предлагает меры.
API только по кнопкам; demo1 и run.py --check не вызывают внешние модели.
Планы хранятся в сессии; публичный deploy не найден в репозитории.

## Грабли
После изменения Python-модулей при разработке перезапустить Streamlit/страницу.
Ключи остаются в локальной .env, в Git только пустой шаблон с auto.

## Проверка
python run.py --check: 187 тестов OK; npm test:16 OK; npm run build:OK; pip check:OK.
Эталон A:95/56.54307/0; demoB:80/56.69056/0. Live A1/A2 прошли без fallback.
Реальные context_lost/webgl_unavailable, retry и камера A/B проверены в браузере.

## Следующий шаг
Завершить чистый запуск и закрыть R с отчётом приёмки.
