# План до 18:00 — 23 сентября, GMT+5

## Команда

| Участник | Git email | Зона ответственности |
|---|---|---|
| @alikhan | achabarovcuru@gmail.com | `citysim/engine.py`, `data.py`, `models.py`, тесты движка, API, запуск/зависимости |
| @AaaDddmyrza | adildaulet2005@gmail.com | `citysim/ai.py`, `app.py`, AI/интерфейс, демонстрация |

Стек: **Python + Streamlit + OpenAI API**. Запуск: **`python run.py`**, без Docker.
Архитектура: **engine (чистые функции: правила → эффекты → Score) → ai (только объяснение готовых чисел, без ключа — demo) → интерфейс**.
UI передаёт выбор в engine; невалидный набор не получает Score и не отправляется в AI.

## Обязательное — сначала сквозной сценарий

| Статус / срок | Этап | Владелец | Проверка готовности |
|---|---|---|---|
| [x] 13:00–13:40 | P0: ТЗ, данные, план, API, запускаемый скелет, README, состояния | @alikhan | `python run.py --check`; старт UI без ключа; baseline 52.5577, Ncrit=2; push |
| [ ] 13:40–14:30 | E1: полный валидатор, лаги, эффекты, синергии, Score | @alikhan | TDD: все правила; эталон 95 / 56.5431 / 0; набор стоимостью 61; порядок не влияет |
| [ ] 13:40–14:30 | U1: форма ровно 5 мер, районы, бюджет, ошибки | @AaaDddmyrza | На моках по API: все 5 направлений доступны; ошибка >100 блокирует расчёт; без повторов |
| [ ] 14:30–15:00 | I1: соединить форму с engine и AI, первый сквозной сценарий | @AaaDddmyrza | Пять мер из эталона → стоимость 95, остаток 5, Score 56.5431 и объяснение; @alikhan сверяет числа |
| [ ] 15:00–15:45 | A1: AI-анализ сильных сторон, рисков, последствий; надёжный fallback | @AaaDddmyrza | `DEMO_MODE=1` без сети; live API при доступе; отказ API → demo; числа только из engine |
| [ ] 15:00–16:15 | E2: границы правил и чисел, трассировка вкладов мер | @alikhan | Бюджет 100/>100, <40/=40, clip, отрицательные эффекты, 3 синергии/конфликта, вход не меняется |
| [ ] 16:30–17:15 | Q1: читаемость UI, демо и обязательные документы | @AaaDddmyrza | Пошаговый сценарий README воспроизводится; ограничения и режим AI видимы |
| [ ] 17:15–17:30 | R1: стоп фичам, сверка README/API/THIRD_PARTY/.env.example | @alikhan | Все обязательные пункты выше проверены, секретов нет, команды актуальны |
| [ ] 17:30–17:50 | R2: чистый клон, приёмка, финальный push | @alikhan | @AaaDddmyrza запускает по README без ключа; после исправлений повтор; main синхронизирован |

## Только после обязательного, по ценности (окно до 16:30)

| Приоритет | Опция | Владелец | Проверка |
|---|---|---|---|
| [ ] 1 | AI-советник: до 10 замен одной меры → engine валидирует/пересчитывает → лучший вариант | @AaaDddmyrza | Лимит 2 раунда; исходный сценарий включён; лучший валиден, Score ≥ исходного; подтверждение пользователем |
| [ ] 2 | Графики изменений районов | @AaaDddmyrza | Значения совпадают с `SimulationResult`, видно до/после и слабейший район |
| [ ] 3 | Воспроизводимые городские события | @alikhan | Отдельный явно отмеченный режим, фиксированные эффекты/seed; официальный режим не меняется |

Если обязательное запаздывает, опции убираем. **17:15 стоп новым фичам; 17:50 финальный push; 18:00 дедлайн.**
Коммит/push после каждого куска и минимум каждый час; за 40 минут без своего коммита — checkpoint по AGENTS.md.
Своё состояние — `docs/state/<ник>.md`; запросы в чужую зону — `docs/HANDOFF.md`.
Общий контракт/типы меняет @alikhan через коммит `API: …`; напарник получает готовый контракт и продолжает независимо.
