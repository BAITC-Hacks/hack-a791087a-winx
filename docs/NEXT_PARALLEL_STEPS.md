# Совместная проверка и следующий параллельный этап

23 сентября 2026. Проверены main `f9e5797` (S1) и
`codex/scenario-flow` `017d6b7` / `e65e2ed` (U1/I1/A1, PR #2).
PR: https://github.com/BAITC-Hacks/hack-a791087a-winx/pull/2

## Что уже сделано

| Владелец | Готовая часть | Где находится |
|---|---|---|
| @alikhan | E1/E2: правила, эффекты, Score; C0: контракты; S1: ограниченный поиск | main |
| @AaaDddmyrza | V1-P: интерактивный 3D baseline | main |
| @AaaDddmyrza | U1: форма пяти решений; I1: сохранённый A; A1: паспорт и объяснение | PR #2, ещё не в main |

V1 целиком не закрыт: 3D пока показывает baseline, даже когда рассчитан A.
Ревизор A2 и визуальное сравнение A/B в V2 ещё не подключены.

## Результат совместной проверки

В отдельной рабочей копии объединены продуктовые файлы двух веток без публикации
слияния. Общая установленная среда Python; это не новый чистый клон с установкой.

- **119 Python-тестов OK**, включая тесты обоих участников и Streamlit AppTest.
- **4 JS-теста OK**. Исходники и bundle 3D в проверяемых ветках одинаковые.
- Дополнительно выполнен путь AppTest: форма → сохранённый A → generate_candidates
  → два review_candidates по 10 → паспорт и demo-объяснение best.
  A: 95 / 56.54307; best из 20: 80 / 56.69056. A не изменён, B автоматически не применён.
- В Chromium заполнен официальный пример и рассчитан A: стоимость 95, остаток 5,
  Score 56.5431, Ncrit=0; паспорт показывает Нура S1 38→48 и S2 35→43.75.
- Два независимых чтения кода не выявили блокирующих ошибок в проверенных путях.
  Live API, физический pinch и потеря WebGL-контекста здесь не проверялись.

## Сначала объединить выполненную работу

PR #2 открыт и конфликтует с main. `git merge-tree` показал конфликты только в
`docs/PLAN.md` и `docs/HANDOFF.md`; продуктовый код объединяется автоматически.
При разрешении сохранить обе завершённые части: S1 и U1/I1/A1, их проверенные факты
и актуальный общий статус. Не заменять целиком документы одной стороной.
IMPLEMENTATION_PLAN объединяется автоматически, но его статусы также нужно прочесть.

Далее общий запуск должен подтвердить 119 Python-тестов и 4 JS-теста.
Этот аудит не выполнял merge PR и не публиковал изменений в ветку напарника.

## Параллельная работа сейчас

Разделение предложено по запросу пользователя. В прежнем плане весь A2 принадлежал
@AaaDddmyrza; теперь серверную часть можно передать @alikhan, оставив UI напарнику.

### @alikhan: A2 — серверная часть ревизора

**Свои файлы:** `citysim/reviewer.py`, `tests/test_reviewer.py`.
Общий API и review_models менять только при необходимости с API-коммитом.
Не редактировать app.py, citysim/ai.py, ui/* и 3D-файлы в этом пакете.

Существующий контракт сохраняется:

```python
review_scenario(source, dataset, constraints, *, demo_mode=True,
                api_key=None, model=None) -> tuple[ReviewResult, Explanation]
```

Критерии готовности:

1. Demo без ключа и сети использует S1; source/constraints проверяются до AI.
2. Live предлагает только ID решений, максимум два раунда по 10 относительно
   первоначального A. Каждый набор проверяется через S1, числа AI игнорируются.
3. Ошибочный JSON/тип не считается проверенным кандидатом; неизвестные строковые
   ID проходят валидатор и сохраняют причины. Лимиты обеспечиваются кодом.
4. Обработаны ошибка/пустой ответ/timeout, сохранён лучший уже проверенный вариант;
   completed_limited и incomplete не смешиваются с improved/cheaper_equal/unchanged.
   Fallback явно обозначен; общий бюджет ревизии и сетевых вызовов — по плану A2.
5. Explanation объясняет именно ревизию A→B и её завершённость.
   `best.score_delta` — изменение относительно baseline, а не относительно A;
   не выдавать его за выигрыш B над A. Один explain_result(best) эту задачу не закрывает.
6. Unit-тесты с моками: demo не создаёт SDK; второй раунд сохраняет A/locked;
   отказ второго раунда сохраняет результат первого; invented Score не используется.

Эта часть не зависит от готовности новой сцены или новых кнопок.

### @AaaDddmyrza: V1 — рассчитанный план в 3D

**Свои файлы:** `app.py`, `ui/scene.py`, `components/city3d/*`,
`tests/test_scene_payload.py`, UI-тесты. `ui/state.py`, `ui/results.py`,
`citysim/ai.py`, README и состояние участника также остаются у него.

Критерии готовности:

1. Сцена получает baseline и сохранённый A по ScenePayload v1; переключение
   состояния меняет показатели и подпись. Черновик не подменяет A в сцене.
2. Для официального A в Нуре видны S1=48 и S2=43.75, а при baseline — 38 и 35.
   Все десять показателей и районные баллы совпадают с паспортом engine.
3. Builder перестаёт фиксировать id/label baseline для любого входного результата.
   JS проверяет schema_version, обрабатываются render_error и снятие выбора null.
4. Переключение не сбрасывает камеру; выбор района согласован с Python;
   WebGL-fallback сохраняет таблицы/управление. Проверка в браузере и на узкой ширине.
5. Локальный bundle пересобран и сохранён; CDN и Node для обычного запуска не нужны.

Существующий C0 уже описывает A/B. Напарник может подготовить переключатель и
проверять B на снимке simulate в тестах, не ожидая live-ревизора.

## Соединение после двух пакетов

- @AaaDddmyrza: UI A2 (`ui/review.py`, app/state/tests) вызывает review_scenario,
  показывает статус, причины отклонения и сравнение A/B; B применяется отдельной
  кнопкой. Новый A очищает прежние B/ревизию/объяснение.
- @AaaDddmyrza: V2 — A/B и «только различия», одинаковая шкала/геометрия/камера.
- @alikhan: параллельно сверка чисел UI↔S1, проверка offline/ошибок и чистого запуска
  по README. После этого совместная приёмка R и демонстрационный сценарий.

Так текущий параллельный цикл имеет две независимые зоны:
**серверный ревизор у @alikhan, UI и 3D у @AaaDddmyrza**.
