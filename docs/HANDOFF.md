# Передача работы

Начальная заготовка создана @alikhan по общей договорённости. После неё каждый меняет
свою зону из PLAN.md; запросы на чужую правку записываются здесь.

| Кому | Что нужно | Зачем / формат | Статус |
|---|---|---|---|
| @alikhan | Реализовать E1 в `citysim/engine.py` | `validate_scenario` и `simulate` по API v1; убрать NotImplementedError только после тестов | Открыт, следующий этап |
| @AaaDddmyrza | Начать U1 в `app.py` на локальном моке результата | Пять `Decision`, `ValidationResult`, `SimulationResult` из API v1; после E1 подключить engine | Открыт, можно параллельно |

Новый запрос: кому → файл/функция → нужное поведение → формат данных → статус.

Проверка `c11ee30` от @AaaDddmyrza: P0 прошёл 8 тестов на Windows/Python 3.12,
HTTP-проверку и сверку чисел. E1/U1/I1 остаются открытыми. Отчёт: [REVIEW.md](REVIEW.md).
Идея уточнена в [IDEA.md](IDEA.md); продуктовый код и API v1 не менялись.

| Кому | Что нужно | Зачем / формат | Статус |
|---|---|---|---|
| @alikhan | При E1/E2 подтвердить effects и indicator_deltas тестами | Для паспорта: effects до clip, deltas после clip, отдельная синергия; отрицательный T1 у M11; существующий API v1 | Открыт, часть E1/E2; менять контракт не нужно |
| @AaaDddmyrza | При A1 добавить сравнение до/после в объяснение | before/after/score_delta/cost/remaining_budget, слабейший район и конкретные критические показатели; текущий шаблон объясняет только итог | Открыт, после I1 |

## Предложение развития от @alikhan

По запросу пользователя концепция собрана в один документ:
[PROPOSAL.md](PROPOSAL.md) — «Аким — два будущих города»: 3D-макет, сравнение A/B,
проверяемый AI-ревизор и корректная обработка уже оптимального выбора человека.
Документ развивает IDEA.md напарника; TASK/DATASET, продуктовый код и API не менялись.

| Кому | Что нужно | Зачем / формат | Статус |
|---|---|---|---|
| @AaaDddmyrza | Составить план реализации по PROPOSAL.md с учётом текущих IDEA/REVIEW | Этапы, владельцы, зависимости, приёмка; отдельно согласовать API советника/3D; сохранить официальные правила | Выполнено: PLAN.md и IMPLEMENTATION_PLAN.md; новые API пока проектируются в C0 |

## Передача технического плана — @AaaDddmyrza

Идея `7069af2` проверена: правила не меняются; 1 407 050 размещений до фильтров
подтверждены. Риски и решения — в [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md).
Действующий API v1 и продуктовый код не менялись; 8 тестов P0 проходят.

| Кому | Подчасти и файлы | Ближайший результат | Статус |
|---|---|---|---|
| @alikhan | E1/E2: engine + тесты; затем C0: review_models/API, S1: search | Эталон 95/56.5431/0; все правила; затем API-коммит для ревизора и сцены | Открыт, начинать с E1 |
| @AaaDddmyrza | U1/I1/A1: app/ui/ai; затем V1/A2/V2: city3d/reviewer/A-B | Форма пяти решений и сквозной MVP; владелец интеграции Python↔JS | Открыт, U1 параллельно E1 |
| @alikhan | C0: утвердить предложенные ReviewResult/ScenePayload | Поля в техническом плане; EPS=1e-9, оба раунда от исходного A, locked-пара с районом; отдельный API-коммит | Открыт, после E2 |

Данные и ТЗ не редактировать. Каждый правит свою зону; потребитель расширений
проверяет API-коммит перед подключением. Рабочая 3D-сцена входит в обязательные критерии приёмки; полный поиск остаётся необязательным.

## Обязательный 3D и подключение существующего ключа

Пользователь снял отсечение разработки по дедлайну: V1/V2 обязательны; этапы
PLAN/IMPLEMENTATION_PLAN теперь зависят от результатов предыдущих этапов.
Добавлен `scripts/start_live.py`: скрытый ввод ключа без записи в OneDrive/Git,
локальный сервер и прежний серверный OpenAI-адаптер. Инструкция — SECRETS.md.
Offline-тесты проверяют границы ввода; реальный API-ключ, доступ к модели и баланс
не проверены. Engine, действующий API и исходные данные не менялись.
