# Передача работы

Начальная заготовка создана @alikhan по общей договорённости. После неё каждый меняет
свою зону из PLAN.md; запросы на чужую правку записываются здесь.

| Кому | Что нужно | Зачем / формат | Статус |
|---|---|---|---|
| @alikhan | Реализовать E1 в `citysim/engine.py` | `validate_scenario` и `simulate` по API v1; убрать NotImplementedError только после тестов | Открыт, следующий этап |
| @AaaDddmyrza | Начать U1 в `app.py` на локальном моке результата | Пять `Decision`, `ValidationResult`, `SimulationResult` из API v1; после E1 подключить engine | Открыт, можно параллельно |

Новый запрос: кому → файл/функция → нужное поведение → формат данных → статус.

Проверка `c11ee30` от @AaaDddmyrza: P0 прошёл 8 тестов на Windows/Python 3.12,
HTTP-проверку и сверку чисел. E1/U1/I1 остаются открытыми. Отчёт: [REVIEW.md](REVIEW.md).
Идея уточнена в [IDEA.md](IDEA.md); продуктовый код и API v1 не менялись.

| Кому | Что нужно | Зачем / формат | Статус |
|---|---|---|---|
| @alikhan | При E1/E2 подтвердить effects и indicator_deltas тестами | Для паспорта: effects до clip, deltas после clip, отдельная синергия; отрицательный T1 у M11; существующий API v1 | Открыт, часть E1/E2; менять контракт не нужно |
| @AaaDddmyrza | При A1 добавить сравнение до/после в объяснение | before/after/score_delta/cost/remaining_budget, слабейший район и конкретные критические показатели; текущий шаблон объясняет только итог | Открыт, после I1 |

## Предложение развития от @alikhan

По запросу пользователя концепция собрана в один документ:
[PROPOSAL.md](PROPOSAL.md) — «Аким — два будущих города»: 3D-макет, сравнение A/B,
проверяемый AI-ревизор и корректная обработка уже оптимального выбора человека.
Документ развивает IDEA.md напарника; TASK/DATASET, продуктовый код и API не менялись.

| Кому | Что нужно | Зачем / формат | Статус |
|---|---|---|---|
| @AaaDddmyrza | Составить план реализации по PROPOSAL.md с учётом текущих IDEA/REVIEW | Этапы, владельцы, зависимости, приёмка; отдельно согласовать API советника/3D; сохранить официальные правила | Выполнено: PLAN.md и IMPLEMENTATION_PLAN.md; новые API пока проектируются в C0 |

## Передача технического плана — @AaaDddmyrza

Идея `7069af2` проверена: правила не меняются; 1 407 050 размещений до фильтров
подтверждены. Риски и решения — в [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md).
Действующий API v1 и продуктовый код не менялись; 8 тестов P0 проходят.

| Кому | Подчасти и файлы | Ближайший результат | Статус |
|---|---|---|---|
| @alikhan | E1/E2: engine + тесты; затем C0: review_models/API, S1: search | Эталон 95/56.5431/0; все правила; затем API-коммит для ревизора и сцены | Открыт, начинать с E1 |
| @AaaDddmyrza | U1/I1/A1: app/ui/ai; затем V1/A2/V2: city3d/reviewer/A-B | Форма пяти решений и сквозной MVP; владелец интеграции Python↔JS | Открыт, U1 параллельно E1 |
| @alikhan | C0: утвердить предложенные ReviewResult/ScenePayload | Поля в техническом плане; EPS=1e-9, оба раунда от исходного A, locked-пара с районом; отдельный API-коммит | Открыт, после E2 |

Данные и ТЗ не редактировать. Каждый правит свою зону; потребитель расширений
проверяет API-коммит перед подключением. Рабочая 3D-сцена входит в обязательные критерии приёмки; полный поиск остаётся необязательным.

## Обязательный 3D и подключение существующего ключа

Пользователь снял отсечение разработки по дедлайну: V1/V2 обязательны; этапы
PLAN/IMPLEMENTATION_PLAN теперь зависят от результатов предыдущих этапов.
Добавлен `scripts/start_live.py`: скрытый ввод ключа без записи в OneDrive/Git,
локальный сервер и прежний серверный OpenAI-адаптер. Инструкция — SECRETS.md.
Offline-тесты проверяют границы ввода; реальный API-ключ, доступ к модели и баланс
не проверены. Engine, действующий API и исходные данные не менялись.

## 3D-прототип — @AaaDddmyrza

На базе E1 `f6110e7` создана подготовительная часть V1 в `components/city3d/`
и `ui/scene.py`, встроена в app.py. Пользователь разрешил публикацию в ветке
`codex/3d-prototype`; перед коммитом получены E2/C0 до `cee214e`.
Движок/API/датасет в изменениях прототипа не менялись.
Пять районов, baseline, 10 показателей, выбор района JS→Python, камера, локальный bundle.
C0 уже опубликован; baseline использует совместимые поля ScenePayload v1.
Полная поддержка C0 (снимки A/B, снятие выбора, нормализация событий и проверка версии)
остаётся частью V1/V2. Не считать V1/V2 или форму решений выполненными. Подробности и
проверки — `components/city3d/README.md`.

## E1 готов — @alikhan

`validate_scenario` и `simulate` реализованы в `citysim/engine.py` по API v1.
Общие типы и исходные данные не менялись. В API уточнены сочетания ошибок и
канонический порядок effects; импорты и сигнатуры прежние.
21 тест движка проходит: все 9 кодов ошибок, cost повторов/unknown ID,
эталон 95 / 5 / 56.54307 / 0, набор 61, одинаковый полный результат для 120 перестановок.
Предыдущий запрос к @alikhan на реализацию E1 выполнен; E2 проверяется отдельно.

| Кому | Что нужно | Зачем / формат | Статус |
|---|---|---|---|
| @AaaDddmyrza | U1/I1: подключить реальные validate_scenario/simulate | Пять Decision, ошибки из InvalidScenarioError.validation; новая форма может использовать готовый движок | Готово к интеграции |
| @AaaDddmyrza | README и чек-лист E1 в IMPLEMENTATION_PLAN | Убрать утверждения о заглушках engine; отметить E1, сохранив честный статус UI на P0; факты в API и тестах | Запрос владельцу общих документов |

## E2 готов — @alikhan

E1 опубликован в `f6110e7`. Добавлен `tests/test_engine_boundaries.py` (12 тестов).
Полный `python run.py --check`: 45 тестов OK; `pip check`: конфликтов зависимостей нет.
Проверены бюджет 100/101, строгий порог 40 после эффектов, лаги 1/2/3/4,
все три синергии/конфликта, M11 T1=-1.75 и B2=+10.5, независимость словарей.
На синтетических копиях проверено сложение разнонаправленных эффектов до clip
с обеих сторон: effects до ограничения, indicator_deltas после ограничения.
Ранее открытые запросы к @alikhan по E1 и effects/deltas/M11 выполнены.
Датасет, TASK и типы не менялись; отчёт — [ENGINE_REVIEW.md](ENGINE_REVIEW.md).

| Кому | Что нужно | Зачем / формат | Статус |
|---|---|---|---|
| @AaaDddmyrza | При обновлении README/IMPLEMENTATION_PLAN отметить также E2 | Граничные тесты выделены в tests/test_engine_boundaries.py; полный check 45 OK; UI остаётся отдельным U1/I1 | Дополнение к запросу выше |
| @alikhan | Следующим этапом C0 опубликовать review_models/ScenePayload | План проверен; E1/E2 больше не блокируют контракт ревизора/3D | Следующая задача, вне текущих двух этапов |

## C0 готов — @alikhan

Доступен `citysim/review_models.py`: ReviewConstraints, CandidateCheck,
ReviewResult; TypedDict ScenePayload/SceneState/SceneEvents; EPS и лимиты 2×10.
Полный контракт и пример JSON-передачи — [API.md](API.md), разделы C0.
Исходные API v1, датасет и engine не изменены. Поиск S1 пока не реализован.

- `validate_review_input(source, dataset, constraints)` вызывается до поиска/AI:
  baseline и изменённые числа отклоняются, исходник сверяется с simulate.
- `max_changes` сейчас строго int 0/1. locked закрепляет меру вместе с районом.
  `check_candidate_constraints` дополняет, но не заменяет validate_scenario.
- `classify_outcome` всегда сравнивает с первоначальным A. EPS нельзя применять
  цепочкой к предыдущему best; правило выбора между кандидатами уточнено в API.
- `status` и `outcome` независимы: incomplete может сохранять improved.
- Сцена: schema_version=1; состояния baseline/A/B; score — float;
  районы/решения — JSON-массивы, городская цель — null; события описаны в API.
  TypedDict не выполняет runtime-валидацию: builder/render_city остаются задачей V1.

Проверка: 20 новых тестов контракта, полный check 65 OK. Пример сцены из API
выполнен с реальным результатом engine: JSON, пять районов и решений, Score
и независимость вложенных словарей подтверждены. Это не проверка рендера 3D.
Ранее открытый запрос C0 на ReviewResult/ScenePayload выполнен.

| Кому | Что нужно | Зачем / формат | Статус |
|---|---|---|---|
| @AaaDddmyrza | После pull использовать C0 в V1/A2 | Доступны импорты из review_models; описание ScenePayload/событий в API; поиск добавится отдельно в S1 | Готово к подключению |
| @AaaDddmyrza | Актуализировать C0 в IMPLEMENTATION_PLAN и общих документах | max_changes=0/1, EPS относительно A, TypedDict сцены, 20 тестов; текущий engine/API не ломаются | Запрос владельцу документов |
| @alikhan | Реализовать S1 по API C0 | Генерация и проверка максимум 20 уникальных кандидатов, два раунда с неизменным A | Следующий этап |

Запросы на актуализацию README/IMPLEMENTATION_PLAN по E1/E2/C0 выполнены в пакете
V1-P. Совместная проверка после pull: 70 Python-тестов, 4 JS-теста и сборка проходят.
