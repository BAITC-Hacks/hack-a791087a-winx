# auto: use OpenAI when key and model are set; otherwise run locally.
# DEMO_MODE=1 forces offline demo. API errors fall back to local results.
DEMO_MODE=auto
OPENAI_API_KEY=
# Required only for live mode: choose a model available to your API project.
OPENAI_MODEL="gpt-6-luna"
